# Contributing to Ursa

## Setup

Use the repository's Python environment and test workflow.

Representative validation command:

```bash
pytest
```

## Expectations

- Preserve governance and approval controls for high-risk behavior.
- Keep user-facing and operator-facing contracts documented in `README.md`.
- Update `BLINK.md` when build, deploy, rollback, or verification behavior changes.
- Update `CHANGELOG.md` for notable repo changes.

Active unfinished work belongs in the workspace root `ROADMAP.md`.
